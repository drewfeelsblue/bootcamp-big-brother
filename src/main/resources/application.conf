db {
    host = "db"
    port = 5432
    user = "bb_user"
    password = "pswd"
    database-name = "postgres"
    max-sessions = 32

    migration {
        url: "jdbc:postgresql://"${db.host}":"${db.port}"/"${db.database-name}""
        user: ""${db.user}""
        password: ""${db.password}""
    }
}

http {
    server {
        host: "0.0.0.0"
        port: 9001
    }
}